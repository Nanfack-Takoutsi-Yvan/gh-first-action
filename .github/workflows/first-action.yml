# this is a basic workflow to help me understand how it works

name: Fist Workflow

on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: | 
          echo "Hello world"
          echo "I love github workflows"
      - name: Print Goodby
        run: echo "Done - Bye"
